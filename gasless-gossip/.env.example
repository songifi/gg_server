# Application Configuration
# ---------------------
# PORT: The port number the application will listen on
PORT=3000

# NODE_ENV: The environment the application is running in
# Valid values: development, production, test
NODE_ENV=development

# StarkNet Configuration
# ---------------------
# STARKNET_PROVIDER_URL: The URL of the StarkNet provider
# Required for blockchain interactions
STARKNET_PROVIDER_URL=

# STARKNET_NETWORK: The StarkNet network to connect to
# Valid values: mainnet, testnet
STARKNET_NETWORK=testnet

# Database Configuration
# --------------------
# MONGODB_URI: The connection URI for MongoDB
# Format: mongodb://[username:password@]host[:port]/database
MONGODB_URI=mongodb://localhost:27017

# MONGODB_NAME: The name of the MongoDB database
MONGODB_NAME=gasless_gossip

# JWT Configuration
# ---------------
# JWT_SECRET: Secret key for JWT token generation and validation
# IMPORTANT: Must be at least 32 characters long in production
JWT_SECRET=your-super-secret-key-change-in-production

# JWT_EXPIRES_IN: JWT token expiration time
# Format: number + time unit (s: seconds, m: minutes, h: hours, d: days)
JWT_EXPIRES_IN=7d