/**
 * Enum representing the privacy settings of a group.
 */
export enum GroupPrivacy {
    PUBLIC = 'public',
    PRIVATE = 'private',
  }
  
  /**
   * Enum defining possible roles for group members.
   */
  export enum MemberRole {
    ADMIN = 'admin',
    MODERATOR = 'moderator',
    MEMBER = 'member',
  }
  